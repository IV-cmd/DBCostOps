# Logstash Master Configuration for DBCostOps
# Logstash auto-loads all .conf files from the pipeline directory in
# alphabetical order: 01-inputs.conf → 02-filters.conf → 03-outputs.conf
# No @include needed here — @include is only valid in logstash.yml, not in pipeline configs.
